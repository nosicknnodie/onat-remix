generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model File {
  id         String   @id @default(cuid())
  url        String
  key        String
  mimeType   String
  purpose    FilePurposeType  @default(PROFILE)
  size       Int
  isTemp     Boolean  @default(false)
  uploaderId String?
  uploader   User?  @relation("UploadedImages", fields: [uploaderId], references: [id], onDelete: SetNull)
  createdAt  DateTime @default(now())
  owner      User[] @relation("UserImage")
  clubImages      Club[] @relation("ClubImage")
  clubEmblems      Club[] @relation("ClubEmblem")
  posts      Post[] @relation("PostFiles")
  comments   PostComment[] @relation("CommentFiles")
}

enum FilePurposeType {
  PROFILE
  CLUB_IMAGE
  CLUB_EMBLEM
  POST_IMAGE
  MATCH_PHOTO
  DOCUMENT
  COMMENT_IMAGE
}

enum UserRoleType {
  NORMAL
  ADMIN
}

model User {
  id            String    @id @default(cuid())
  nick          String?
  name          String?
  email         String   @unique
  emailVerified DateTime? @map("email_verified")
  password      String?

  role          UserRoleType @default(NORMAL)
  userImageId   String? // 현재 설정된 프로필 이미지
  userImage     File? @relation("UserImage", fields: [userImageId], references: [id])
  uploadedImages   File[] @relation("UploadedImages")
  playerNative  PlayerNativeType? // 선출 중등 고등 대학 프로
  height        Int?       // 키
  birth         String?    // 생일
  gender        GenderType? // 성별
  position1     PositionType?
  position2     PositionType?
  position3     PositionType?
  si            String?     // 도시 (시도)
  gun           String?     // 지역 (시군구)
  clothesSize   String?        // 옷 사이즈
  shoesSize     String?        // 신발 사이즈

  ownerClubs    Club[]  @relation("ClubOwnerUser")
  createClubs   Club[]  @relation("ClubCreateUser")
  players       Player[]
  // toProposals   Proposal[]  @relation("toProposal")
  // fromProposals Proposal[]  @relation("fromProposal")
  mercenarys    Mercenary[]
  // applications  Application[]
  playerLogs    PlayerLog[]
  comments      Comment[]
  toReplies     Comment[] @relation("CommentReplyToUser")
  matchs        Match[]
  evaluations   Evaluation[]
  postComments  PostComment[]
  posts         Post[]
  postLikes     PostLike[]
  postVotes     PostVote[]
  commentVotes  CommentVote[]

  session Session[]
  key     Key[]

  createdAt     DateTime @default(now())
  updatedAt     DateTime @default(now()) @updatedAt
  
  @@map("users")
}

enum TokenType {
  VERIFICATION
  PASSWORD_RESET
  TWO_FACTOR
}

enum PlayerNativeType {
  NO          // 비선출
  MIDDLE      // 중등
  HIGH        // 고등
  COLLEAGE    // 대학
  PRO         // 프로
}

enum GenderType {
  MALE    // 남자
  FEMALE  // 여자
}


model ConfirmToken {
  id String @id @default(cuid())
  email String
  token String @unique
  expires DateTime
  type  TokenType @default(VERIFICATION)
  @@unique([email, token, type])
}

model Session {
  id        String   @id
  userId    String
  expiresAt DateTime
  user      User     @relation(references: [id], fields: [userId], onDelete: Cascade)
}

model Key {
  id              String  @id
  hashedPassword String?
  userId          String
  user            User    @relation(references: [id], fields: [userId], onDelete: Cascade)

  @@unique([id])
} 


model Club {
  id            String    @id @default(cuid())
  name          String    // 클럽 이름
  description   String?   @db.Text
  si            String?   // 도시
  gun           String?   // 지역
  imageId       String?   // 클럽 이미지
  emblemId      String?   // 엠블럼 URL
  image         File?      @relation("ClubImage",fields: [imageId], references: [id], onDelete: SetNull)
  emblem        File?      @relation("ClubEmblem",fields: [emblemId], references: [id], onDelete: SetNull)


  isPublic      Boolean?  @default(false)
  ownerUserId  String?
  ownerUser    User?      @relation("ClubOwnerUser",fields: [ownerUserId], references: [id], onDelete: SetNull)
  createUserId  String?
  createUser    User?     @relation("ClubCreateUser",fields: [createUserId], references: [id], onDelete: SetNull)
  players       Player[]      // 회원 선수
  matchClubs    MatchClub[]       // 매치
  mercenarys    Mercenary[]     // 용병
  boards        Board[]       // 게시판
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @default(now()) @updatedAt

  @@index([createUserId])

}


enum JobTitle {
  CHAIRMAN      // 회장
  VICE_CHAIRMAN // 부회장
  DIRECTOR      // 감독
  COACH         // 코치
  OPERATOR      // 운영
  ADVISER       // 고문
  NO            // 없음
}

model Mercenary {
  id              String    @id @default(cuid())
  name            String
  hp              String?
  description     String?
  position1       PositionType?
  position2       PositionType?
  position3       PositionType?

  clubId          String                 // 🔄 클럽 직접 연결
  club            Club   @relation(fields: [clubId], references: [id], onDelete: Cascade)

  userId          String?
  user            User?  @relation(fields: [userId], references: [id], onDelete: SetNull)       
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @default(now()) @updatedAt
  
  attendances      Attendance[]

  @@index([userId])
  @@unique([userId, clubId])
}

model Player {
  id              String    @id @default(cuid())
  nick            String
  userId          String?
  jobTitle        JobTitle? @default(NO)
  role            RoleType  @default(PENDING)
  permissions     PlayerPermission[]
  clubId          String

  isInjury        Boolean  @default(false)
  isRest          Boolean  @default(false)
  status          StatusType @default(PENDING)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @default(now()) @updatedAt
  
  user              User?     @relation(fields: [userId], references: [id], onDelete: SetNull)
  club              Club     @relation(fields: [clubId], references: [id], onDelete: Cascade)
  attendances       Attendance[]
  playerLogs        PlayerLog[]
  createdPlayerLogs PlayerLog[] @relation("PlayerLogCreatePlayer")
  createdMatches    Match[] @relation("MatchCreatePlayer")
  comments          Comment[]
  toReplies         Comment[] @relation("CommentReplyToPlayer")
  posts             Post[]
  postComments      PostComment[]

  @@index([clubId])
  @@index([userId])
  @@unique([clubId, userId])
}

enum StatusType {
  PENDING
  APPROVED
  REJECTED
  LEFT
  BANNED
  CANCELLED
}

enum RoleType {
  MASTER
  MANAGER
  NORMAL
  PENDING
  NO
}

// 클럽 내 역할별 세부 권한 키
enum PlayerPermissionKey {
  CLUB_VIEW
  CLUB_MANAGE
  PLAYER_MANAGE
  PLAYER_ASSIGN_MANAGER
  PLAYER_APPROVE_MEMBER
  PLAYER_VIEW
  MATCH_CREATE
  MATCH_MANAGE
  MATCH_MASTER
  MERCENARY_MANAGE
  BOARD_MASTER
  BOARD_MANAGER
  BOARD_NORMAL
  BOARD_PENDING
  EVALUATION_VIEW
}

// Player 역할(RoleType)과 기능 권한 매핑 템플릿
model PlayerRolePermissionTemplate {
  id          String               @id @default(cuid())
  role        RoleType
  permission  PlayerPermissionKey
  allowed     Boolean              @default(false)
  createdAt   DateTime             @default(now())
  updatedAt   DateTime             @default(now()) @updatedAt

  @@unique([role, permission])
}

// 개별 Player에 부여된 세부 권한
model PlayerPermission {
  id          String               @id @default(cuid())
  playerId    String
  player      Player               @relation(fields: [playerId], references: [id], onDelete: Cascade)
  permission  PlayerPermissionKey
  allowed     Boolean              @default(false)
  // role 기반 일괄 부여인지, 수동 오버라이드인지 추적용
  sourceRole  RoleType?
  createdAt   DateTime             @default(now())
  updatedAt   DateTime             @default(now()) @updatedAt

  @@unique([playerId, permission])
  @@index([playerId])
}

enum PlayerLogType {
  STATUS      // 가입, 탈퇴
  REST        // 휴식기
  INJURY      // 부상
  ROLE        // 역할
  JOB_TITLE   // 직책
}
enum PlayerLogValue {
  START     // 시작
  END       // 종료
  CHANGED   // 변경
}

model PlayerLog {
  id              String    @id @default(cuid())
  playerId        String
  player          Player   @relation(fields: [playerId], references: [id], onDelete: Cascade)
  type            PlayerLogType?
  value           PlayerLogValue?
  from            String?
  to              String?
  createUserId    String?
  createUser      User?   @relation(fields: [createUserId], references: [id], onDelete: SetNull)
  createPlayerId  String?
  createPlayer    Player? @relation("PlayerLogCreatePlayer", fields: [createPlayerId], references: [id], onDelete: SetNull)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @default(now()) @updatedAt

  @@index([playerId])
  @@index([createUserId])
  @@index([createPlayerId])
}




model Match {
  id              String        @id @default(cuid())
  
  title           String
  description     String     @db.Text
  placeName       String?
  address         String?
  lng             Float?      
  lat             Float?     
  stDate          DateTime
  
  matchClubs      MatchClub[]
  createUserId    String?
  createUser      User?    @relation(fields: [createUserId], references: [id], onDelete: SetNull)
  createPlayerId  String?
  createPlayer    Player?  @relation("MatchCreatePlayer", fields: [createPlayerId], references: [id], onDelete: SetNull)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @default(now()) @updatedAt

}


model MatchClub {
  id            String        @id @default(cuid())
  matchId       String        
  match         Match         @relation(fields: [matchId], references: [id], onDelete: Cascade)
  clubId        String
  club          Club          @relation(fields: [clubId], references: [id], onDelete: Cascade)
  isSelf        Boolean       @default(false)   // 자체전 여부
  attendances   Attendance[]
  quarters      Quarter[]
  teams         Team[]
  evaluations   Evaluation[]
  total         MatchClubStatsTotal?
  totalHistory  MatchClubStatsHistory?

  isUse         Boolean     @default(true)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @default(now()) @updatedAt

  @@index([clubId])
  @@index([matchId])
  @@unique([matchId, clubId])
}


model MatchClubStatsTotal {
  matchClubId String   @id
  matchClub   MatchClub @relation(fields: [matchClubId], references: [id], onDelete: Cascade)

  playerCount Int @default(0)   // 회원수
  voteCount   Int @default(0)   // 참석수
  voteRate    Int @default(0)   // 참석률
  checkCount  Int @default(0)   // 출석수
  checkRate   Int @default(0)   // 출석률
  mercenaryVoteCount  Int @default(0)   // 용병 참석수
  mercenaryCheckCount  Int @default(0)  // 용병 출석수
}

model MatchClubStatsHistory {
  matchClubId String   @id
  matchClub   MatchClub @relation(fields: [matchClubId], references: [id], onDelete: Cascade)

  monthlyVoteCount   Int @default(0)   // 월별 평균 참석수
  monthlyVoteRate    Int @default(0)   // 월별 평균 참석률
  monthlyCheckCount  Int @default(0)   // 월별 평균 출석수
  monthlyCheckRate   Int @default(0)   // 월별 평균 출석률

  quarterlyVoteCount   Int @default(0)   // 분기별 평균 참석수
  quarterlyVoteRate    Int @default(0)   // 분기별 평균 참석률
  quarterlyCheckCount  Int @default(0)   // 분기별 평균 출석수
  quarterlyCheckRate   Int @default(0)   // 분기별 평균 출석률

  halfYearVoteCount   Int @default(0)   // 반기별 평균 참석수
  halfYearVoteRate    Int @default(0)   // 반기별 평균 참석률
  halfYearCheckCount  Int @default(0)   // 반기별 평균 출석수
  halfYearCheckRate   Int @default(0)   // 반기별 평균 출석률

  yearVoteCount   Int @default(0)   // 연도별 평균 참석수
  yearVoteRate    Int @default(0)   // 연도별 평균 참석률
  yearCheckCount  Int @default(0)   // 연도별 평균 출석수
  yearCheckRate   Int @default(0)   // 연도별 평균 출석률
}


model Team {
  id              String  @id @default(cuid())
  seq             Int @default(0)
  name            String
  color           String

  quartersAsTeam1        Quarter[] @relation("QuarterTeam1")
  quartersAsTeam2        Quarter[] @relation("QuarterTeam2")
  
  attendances     Attendance[]
  matchClubId         String
  matchClub           MatchClub @relation(fields: [matchClubId], references: [id], onDelete: Cascade)
  assigned        Assigned[]
  records         Record[]
  createdAt       DateTime @default(now())
  updatedAt       DateTime @default(now()) @updatedAt
  @@index([matchClubId])
}

enum AttendanceState {
  NORMAL    // 기본값
  EXCUSED   // 불참.
  RETIRED   // 떠남.
}

model Attendance {
  id          String    @id @default(cuid())
  isVote      Boolean   @default(false)   // 참석투표 여부
  voteTime    DateTime?                   // 참석투표 시간

  isCheck     Boolean   @default(false)   // 출석체크
  checkTime   DateTime?                   // 출석체크 시간

  state       AttendanceState @default(NORMAL)
  
  playerId        String?   // 선수 fkey
  mercenaryId     String?   // 용병 fkey
  matchClubId     String    // 매치 fkey
  teamId          String?   // 팀 fkey

  player      Player?     @relation(fields: [playerId], references: [id], onDelete: SetNull)
  mercenary   Mercenary?  @relation(fields: [mercenaryId], references: [id], onDelete: SetNull)
  team        Team?       @relation(fields: [teamId], references: [id], onDelete: SetNull)
  matchClub   MatchClub       @relation(fields: [matchClubId], references: [id], onDelete: Cascade)

  ratingStats   AttendanceRatingStats?
  ratingVote    AttendanceRatingVote?
  ratingHistory AttendanceRatingHistory?
  assigneds   Assigned[]
  evaluations Evaluation[]
  records     Record[]
  assistRecords Record[] @relation("AssistAttendance")
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @default(now()) @updatedAt

  @@index([playerId])
  @@index([teamId])
  @@index([matchClubId])
  @@index([mercenaryId])
  @@unique([matchClubId, mercenaryId])
  @@unique([matchClubId, playerId])
}

model AttendanceRatingStats {
  attendanceId String @id
  attendance   Attendance @relation(fields: [attendanceId], references: [id], onDelete: Cascade)

  averageRating Int  @default(0) // 해당 선수의 평균 평점(정수로 저장, 필요 시 100배 등 스케일 사용)
  totalRating   Int  @default(0) // 해당 선수의 평점 합계
  voterCount    Int  @default(0) // 몇 명이 평점을 남겼는지
  likeCount     Int  @default(0) // 받은 좋아요 누적
}

model AttendanceRatingVote {
  attendanceId String @id
  attendance   Attendance @relation(fields: [attendanceId], references: [id], onDelete: Cascade)

  totalUsedRating  Int     @default(0) // 본인을 제외하고 사용한 평점 합계
  hasVoted         Boolean @default(false) // 평점을 등록했는지 여부
  votedMemberCount Int     @default(0) // 본인을 제외한 몇 명에게 평점을 넣었는지
  usedLikeCount    Int     @default(0) // 사용한 좋아요 개수
}

model AttendanceRatingHistory {
  attendanceId String @id
  attendance   Attendance @relation(fields: [attendanceId], references: [id], onDelete: Cascade)

  monthlyAverageRating Int @default(0)    // 최근 월별 평균 평점
  monthlyTotalRating   Int @default(0)    // 최근 월별 합계 평점
  quarterlyAverageRating Int @default(0)  // 최근 분기별 평균 평점
  quarterlyTotalRating   Int @default(0)  // 최근 분기별 합계 평점
  halfYearAverageRating  Int @default(0)  // 최근 상/하반기별 평균 평점
  halfYearTotalRating    Int @default(0)  // 최근 상/하반기별 합계 평점
  yearlyAverageRating    Int @default(0)  // 최근 연도별 평균 평점
  yearlyTotalRating      Int @default(0)  // 최근 연도별 합계 평점
}

// 쿼터정보
model Quarter {
  id            String    @id @default(cuid())
  order         Int
  isSelf        Boolean   @default(false)
  // 자체전일 경우에만 설정
  team1Id    String?
  team2Id    String?
  team1      Team? @relation("QuarterTeam1", fields: [team1Id], references: [id], onDelete: SetNull)
  team2      Team? @relation("QuarterTeam2", fields: [team2Id], references: [id], onDelete: SetNull)


  matchClubId       String
  matchClub         MatchClub     @relation(fields: [matchClubId], references: [id], onDelete: Cascade)
  assigneds      Assigned[]
  records       Record[]
  
  @@index([matchClubId])
}

// 쿼터 등록 선수 정보 (쿼터별 선수 포지션)
model Assigned {
  id            String    @id @default(cuid())
  position      PositionType
  quarterId     String
  attendanceId  String 
  teamId        String?
  team          Team?       @relation(fields: [teamId], references: [id], onDelete: SetNull)
  quarter       Quarter     @relation(fields: [quarterId], references: [id], onDelete: Cascade)
  attendance    Attendance  @relation(fields: [attendanceId], references: [id], onDelete: Cascade)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @default(now()) @updatedAt

  @@index([attendanceId])
  @@index([quarterId])
  @@index([teamId])
  @@unique([quarterId, attendanceId])

}


// 코멘트
model Comment {
  id            String    @id @default(cuid())
  content       Json
  isDeleted     Boolean   @default(false)

  userId        String?
  user          User?   @relation(fields: [userId], references: [id], onDelete: SetNull)
  playerId      String?
  player        Player? @relation(fields: [playerId], references: [id], onDelete: SetNull)

  targetId      String
  targetType    CommentTargetType
  parentId      String?
  parentComment Comment?  @relation("CommentToComment", fields: [parentId], references: [id], onDelete: Restrict, onUpdate: Restrict)
  replys        Comment[] @relation("CommentToComment")

  // 👇 자동 멘션 대상(대댓글 이상에서 사용)
  replyToUserId String?
  replyToUser   User?    @relation("CommentReplyToUser", fields: [replyToUserId], references: [id], onDelete: SetNull)
  replyToPlayerId String?
  replyToPlayer   Player?    @relation("CommentReplyToPlayer", fields: [replyToPlayerId], references: [id], onDelete: SetNull)

  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @default(now()) @updatedAt

  @@index([targetId, targetType])
  @@index([userId])
  @@index([playerId])
  @@index([parentId])

}

enum CommentTargetType {
  MATCH_CLUB
}

enum PositionType {
  LS
  ST
  RS
  LW
  LF
  CF
  RF
  RW
  LAM
  CAM
  RAM
  LM
  LCM
  CM
  RCM
  RM
  LWB
  LDM
  DM
  RDM
  RWB
  LB
  LCB
  SW
  RCB
  RB
  GK
}


model Record {
  id            String   @id @default(cuid())

  attendanceId    String
  attendance      Attendance @relation(fields: [attendanceId], references: [id], onDelete: Cascade)

  assistAttendanceId String?  // 어시스트도 Attendance 기반으로 추적
  assistAttendance   Attendance? @relation("AssistAttendance", fields: [assistAttendanceId], references: [id], onDelete: SetNull)
  
  teamId        String?
  team          Team?     @relation(fields: [teamId], references: [id], onDelete: SetNull)

  quarterId     String
  quarter       Quarter  @relation(fields: [quarterId], references: [id], onDelete: Cascade)

  isOwnGoal     Boolean   @default(false)
  goalType      GoalType @default(NORMAL)

  createdAt     DateTime  @default(now())

  @@index([attendanceId])
  @@index([assistAttendanceId])
}

enum GoalType {
  NORMAL
  PK
  HEADER
  OWNGOAL
}

// 평가
model Evaluation {
  id             String   @id @default(cuid())
  userId         String?
  user           User?     @relation(fields: [userId], references: [id], onDelete: SetNull)

  attendanceId   String
  attendance     Attendance @relation(fields: [attendanceId], references: [id], onDelete: Cascade)

  matchClubId    String
  matchClub      MatchClub @relation(fields: [matchClubId], references: [id], onDelete: Cascade)

  score          Int      @default(0)       // 0~60 중 하나
  liked          Boolean  @default(false)   // 좋아요 여부

  createdAt      DateTime @default(now())

  @@unique([userId, matchClubId, attendanceId])
}

enum BoardType {
  TEXT      // 일반 텍스트 게시판
  GALLERY   // 이미지 중심
  VIDEO     // 영상 중심
  NOTICE    // 공지
  LINK      // 링크 또는 자료
  ARCHIVE   // 자료실
}

enum BoardPermissionAction {
  READ
  WRITE
  MANAGE
}

model Board {
  id        String     @id @default(cuid())
  name      String
  slug      String?
  order     Int        @default(0)
  isUse     Boolean    @default(true)
  type      BoardType  @default(TEXT)

  clubId    String?
  clubs     Club?      @relation(fields: [clubId], references: [id], onDelete: Cascade)

  readRole    UserRoleType?  @default(NORMAL)
  writeRole   UserRoleType?  @default(NORMAL)

  permissions BoardPermission[]
  posts     Post[]
  createdAt DateTime   @default(now())

  @@unique([slug, clubId])
}

model BoardPermission {
  id         String                @id @default(cuid())
  boardId    String
  board      Board                 @relation(fields: [boardId], references: [id], onDelete: Cascade)
  action     BoardPermissionAction
  permission PlayerPermissionKey
  allowed    Boolean               @default(true)
  createdAt  DateTime              @default(now())
  updatedAt  DateTime              @default(now()) @updatedAt

  @@unique([boardId, action, permission])
  @@index([boardId])
}

model Post {
  id        String   @id @default(cuid())
  title     String
  content   Json?
  state     PostState @default(DRAFT)

  authorId  String?
  author    User?     @relation(fields: [authorId], references: [id], onDelete: SetNull)
  authorPlayerId  String?
  authorPlayer    Player?     @relation(fields: [authorPlayerId], references: [id], onDelete: SetNull)

  boardId   String?
  board     Board?    @relation(fields: [boardId], references: [id], onDelete: Cascade)

  comments  PostComment[]
  likes     PostLike[]
  votes     PostVote[]

  files     File[] @relation("PostFiles")

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  @@index([authorId])
  @@index([authorPlayerId])
  @@index([boardId])
}

model PostComment {
  id        String   @id @default(cuid())
  indexId   String
  depth     Int      @default(0)
  path      String
  voteCount Int      @default(0)

  content   Json?   
  isDeleted Boolean  @default(false)

  postId    String
  post      Post     @relation(fields: [postId], references: [id], onDelete: Cascade)

  authorId  String?
  author    User?     @relation(fields: [authorId], references: [id], onDelete: SetNull)
  
  authorPlayerId  String?
  authorPlayer    Player?     @relation(fields: [authorPlayerId], references: [id], onDelete: SetNull)


  parentId  String?        // 대댓글 기능

  votes     CommentVote[]
  files     File[]   @relation("CommentFiles")

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  @@unique([parentId, indexId])
  @@index([path])
  @@index([postId])
  @@index([authorId])
  @@index([authorPlayerId])
  @@index([parentId])
}

model CommentVote {
  id          String   @id @default(cuid())

  commentId   String
  comment     PostComment @relation(fields: [commentId], references: [id], onDelete: Cascade)

  userId      String?
  user        User?   @relation(fields: [userId], references: [id], onDelete: SetNull)

  vote        Int    // 예: +1 (upvote), -1 (downvote)

  createdAt   DateTime @default(now())

  @@unique([commentId, userId])
  @@index([commentId])
}


model PostVote {
  id       String @id @default(cuid())

  postId   String
  post     Post   @relation(fields: [postId], references: [id], onDelete: Cascade)

  userId   String?
  user     User?   @relation(fields: [userId], references: [id], onDelete: SetNull)

  vote      Int
  createdAt DateTime @default(now())

  @@unique([postId, userId])
  @@index([postId])
}

model PostLike {
  id       String @id @default(cuid())

  postId   String
  post     Post   @relation(fields: [postId], references: [id], onDelete: Cascade)

  userId   String?
  user     User?   @relation(fields: [userId], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())

  @@unique([postId, userId])
  @@index([postId])
}

enum PostState {
  DRAFT
  PUBLISHED
  DELETED
}
